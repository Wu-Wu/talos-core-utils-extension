name: coreutils
variant: scratch
shell: /bin/bash
dependencies:
  - stage: base
steps:
  - sources:
      - url: https://ftp.gnu.org/gnu/coreutils/coreutils-{{ .COREUTILS_VERSION }}.tar.xz
        destination: coreutils.tar.xz
        sha256: {{ .COREUTILS_SHA256 }}
        sha512: {{ .COREUTILS_SHA512 }}
    prepare:
      - |
        tar -xJf coreutils.tar.xz --strip-components=1
      - |
        mkdir build
        cd build
        ../configure \
            --prefix=/usr/local \
            --disable-nls \
            --without-gmp \
            --without-libattr \
            --without-libacl
    build:
      - |
        cd build
        make -j $(nproc) cat
    install:
      - |
        cd build
        mkdir -p /rootfs/usr/local/bin
        cp src/cat /rootfs/usr/local/bin/cat
        strip /rootfs/usr/local/bin/cat
    test:
      - |
        mkdir -p /extensions-validator-rootfs
        cp -r /rootfs/ /extensions-validator-rootfs/rootfs
        cp /pkg/manifest.yaml /extensions-validator-rootfs/manifest.yaml
        /extensions-validator validate --rootfs=/extensions-validator-rootfs --pkg-name="${PKG_NAME}"
finalize:
  - from: /rootfs
    to: /rootfs
  - from: /pkg/manifest.yaml
    to: /